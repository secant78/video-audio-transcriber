# Core dependencies
faster-whisper>=1.0.0
openai>=1.0.0
gradio>=4.0.0
groq>=1.0.0

# Whisper's audio backend (ffmpeg Python bindings)
# NOTE: you also need ffmpeg installed as a system binary.
#   Windows: winget install ffmpeg   OR   choco install ffmpeg
#   Mac:     brew install ffmpeg
#   Linux:   sudo apt install ffmpeg
